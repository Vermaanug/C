#include <stdio.h>
int main()
{
    int radius = 3;
    float pi = 3.14;

    printf("Area of circle is %f\n", pi * radius * radius);

    int height = 3;

    printf("Volume is cyclinder is %f\n", pi * radius * radius * height);
    return 0;
}